import { LogoContainer, LogoIcon, LogoText } from "./Logo.styled";

function Logo() {
  return (
    <LogoContainer>
      <LogoIcon />
      <LogoText>VitLife</LogoText>
    </LogoContainer>
  );
}

export default Logo;
